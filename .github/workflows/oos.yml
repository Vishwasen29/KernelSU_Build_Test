name: KSUN Build OOS Stock OPlus SM8250 Kernel

on:
  workflow_dispatch:

env:
  TZ: Asia/Shanghai
  CLANG_VERSION: clang-r547379

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set swap to 10G
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 10

      - name: Setup build environment
        run: |
          echo "BUILD_TIME=$(date +%y%m%d)" >> $GITHUB_ENV
          echo "BUILD_TIME_1=$(date +%Y-%m-%d)" >> $GITHUB_ENV

          sudo apt-get update
          sudo apt-get install -y \
            git ccache automake flex lzop bison gperf build-essential \
            zip curl zlib1g-dev g++-multilib bzip2 libbz2-dev liblz4-tool \
            make squashfs-tools dpkg-dev libssl-dev python3 python-is-python3 bc \
            libc6-dev-i386 libncurses-dev wget

          mkdir -p kernel_workspace

      - name: Cache Clang toolchain
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/kernel_workspace/clang-aosp
          key: ${{ env.CLANG_VERSION }}

      - name: Download Clang (if cache miss)
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          cd kernel_workspace
          mkdir clang-aosp
          wget -O ${CLANG_VERSION}.tar.gz \
            https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${CLANG_VERSION}.tar.gz
          tar -xf ${CLANG_VERSION}.tar.gz -C clang-aosp

      - name: Clone kernel source
        run: |
          cd kernel_workspace
          # Avoid --recursive; fetch only what's needed to keep the clone fast
          git clone --depth=1 \
            https://github.com/Vishwasen29/android_kernel_oneplus_sm8250 \
            -b op8t android-kernel

      - name: Setup KernelSU-Next + Baseband-guard
        run: |
          cd kernel_workspace/android-kernel

          curl -LSs \
            https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next/kernel/setup.sh \
            | bash -s legacy

          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash

          sed -i 's/ -dirty//g' scripts/setlocalversion

          RSKSUVER=$(cd KernelSU-Next && expr $(git rev-list --count HEAD) + 30000)
          echo "RSKSUVER=$RSKSUVER" >> $GITHUB_ENV

          #bash $GITHUB_WORKSPACE/patches/kernel-4.19_5.4-patch.sh  || true
          # Enable KernelSU manual hook
          #echo "CONFIG_KSU_MANUAL_HOOK=y" >> arch/arm64/configs/vendor/kona-perf_defconfig
        

          # Create debugfs config fragment
          mkdir -p arch/arm64/configs/vendor
          echo "CONFIG_DEBUG_FS=y" > arch/arm64/configs/vendor/debugfs.config

      - name: Build kernel
        run: |
          cd kernel_workspace/android-kernel

          export ARCH=arm64
          export SUBARCH=arm64
          export BRAND_SHOW_FLAG=oneplus

          export PATH=$GITHUB_WORKSPACE/kernel_workspace/clang-aosp/bin:$PATH

          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-

          export KBUILD_BUILD_HOST=Github-Action
          export KBUILD_BUILD_USER=$(echo ${{ github.actor }} | tr A-Z a-z)

          # Generate base defconfig
          make O=out \
            ARCH=arm64 \
            CC=clang \
            LLVM=1 \
            LLVM_IAS=1 \
            vendor/kona-perf_defconfig

          # FIX: Merge the debugfs config fragment into the generated .config
          ./scripts/kconfig/merge_config.sh -m -O out \
            out/.config \
            arch/arm64/configs/vendor/debugfs.config

          # Sync the merged config
          make O=out \
            ARCH=arm64 \
            CC=clang \
            LLVM=1 \
            LLVM_IAS=1 \
            olddefconfig

          # Full build
          make -j$(nproc) \
            O=out \
            ARCH=arm64 \
            CC=clang \
            LLVM=1 \
            LLVM_IAS=1

      - name: Verify kernel output
        run: |
          cd kernel_workspace/android-kernel/out

          if [ -f arch/arm64/boot/Image ]; then
            echo "CHECK_IMAGE=true" >> $GITHUB_ENV
            echo "KERNEL_VERSION=$(cat include/config/kernel.release)" >> $GITHUB_ENV
          else
            echo "Kernel output file is empty"
            exit 1
          fi

      - name: Prepare AnyKernel3 and package
        if: env.CHECK_IMAGE == 'true'
        run: |
          cd kernel_workspace
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3

          sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
          sed -i 's!BLOCK=.*!BLOCK=auto;!g' AnyKernel3/anykernel.sh
          sed -i 's/IS_SLOT_DEVICE=0;/IS_SLOT_DEVICE=auto;/g' AnyKernel3/anykernel.sh

          rm -rf AnyKernel3/.git* AnyKernel3/README.md

          cp android-kernel/out/arch/arm64/boot/Image AnyKernel3/

          # FIX: Copy DTB for SM8250 so AnyKernel3 can flash it alongside the kernel
          if [ -f android-kernel/out/arch/arm64/boot/dtb ]; then
            cp android-kernel/out/arch/arm64/boot/dtb AnyKernel3/
          fi

          cd AnyKernel3
          zip -r AK3-OPlus-SM8250-KSU_${{ env.RSKSUVER }}.zip ./*

      - name: Upload Image artifact
        if: env.CHECK_IMAGE == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: Image
          path: kernel_workspace/android-kernel/out/arch/arm64/boot/Image

      - name: Upload AnyKernel3 ZIP
        if: env.CHECK_IMAGE == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: AK3
          path: kernel_workspace/AnyKernel3/*.zip
