diff --git a/fs/namespace.c b/fs/namespace.c
index XXXXXXX..YYYYYYY 100644
--- a/fs/namespace.c
+++ b/fs/namespace.c
@@ -3445,6 +3445,15 @@ static int path_mount(const char *dev_name, struct path *path,
     int ret;

+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+    if (likely(susfs_is_current_proc_umounted())) {
+        if (dev_name && strstr(dev_name, MAGIC_MOUNT_WORKDIR)) {
+            return -EPERM;
+        }
+    }
+#endif
+
     ret = do_new_mount(&path, type_page, sb_flags,
                        mnt_flags, dev_name, data_page);
